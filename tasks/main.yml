- name: install cronie package
  yum:
    name: cronie

- name: create supervisor config
  copy:
    src: cron.ini
    dest: /etc/supervisord.d/cron.ini

- name: start cron
  supervisorctl:
    name: crond
    state: present
